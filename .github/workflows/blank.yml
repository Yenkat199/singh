name: sync fork

on:
  schedule:
  - cron: "0 2 * * 1-5"
jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
        with:
        # URL of gitHub public upstream repo
        upstream_repo: https://github.com/singh/.github/workflows
        # Branch to merge from upstream (defaults to downstream branch)
        upstream_branch: uat
        # Branch to merge into downstream
        downstream_branch: uat
        # GitHub Bot token
        token: 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
    
